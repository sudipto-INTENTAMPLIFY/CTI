name: RC11 Staging Evidence

on:
  push:
    branches: [RC11]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  artifact-and-static-evidence:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Build RC11 staging artifact
        run: node scripts/build-rc11-staging.mjs
      - name: Verify RC11 static evidence
        run: node scripts/verify-rc11.mjs
      - name: Upload staging artifact
        uses: actions/upload-artifact@v4
        with:
          name: rc11-staging-${{ github.sha }}
          path: dist/
          if-no-files-found: error
          retention-days: 14

  live-staging-evidence:
    needs: artifact-and-static-evidence
    runs-on: ubuntu-latest
    env:
      STAGING_URL: https://staging.cybertechintelligence.com
    steps:
      - name: Verify staging origin
        run: |
          set -euo pipefail
          curl -fsS "$STAGING_URL/" -o /tmp/index.html
          curl -fsS "$STAGING_URL/RC11_RUNTIME.js" | grep -q "RC11.1"
          curl -fsS "$STAGING_URL/data/evidence-graph.json" | grep -q '"schema": "cti.evidence_graph.v1"'
          curl -fsSI "$STAGING_URL/" > /tmp/headers.txt
          echo "Staging static origin checks PASS: $STAGING_URL"
